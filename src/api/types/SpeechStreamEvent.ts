// This file was auto-generated by Fern from our API Definition.

import type * as Speechify from "../index.js";

/**
 * One event on the POST /v1/audio/stream/with-timestamps stream. The
 * `type` field discriminates the variants and mirrors the SSE `event:`
 * name, so an event is identifiable from its `data:` payload alone.
 */
export type SpeechStreamEvent =
    | Speechify.SpeechStreamEvent.SpeechChunk
    | Speechify.SpeechStreamEvent.SpeechDone
    | Speechify.SpeechStreamEvent.SpeechError;

export namespace SpeechStreamEvent {
    export interface SpeechChunk extends Speechify.SpeechChunkEvent {
        type: "speech.chunk";
    }

    export interface SpeechDone extends Speechify.SpeechDoneEvent {
        type: "speech.done";
    }

    export interface SpeechError extends Speechify.SpeechErrorEvent {
        type: "speech.error";
    }
}
